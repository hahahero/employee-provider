<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mysql.demo.dao.EmployeesDao">

    <resultMap type="com.mysql.demo.entity.Employees" id="EmployeesMap">
        <result property="empNo" column="emp_no" jdbcType="INTEGER"/>
        <result property="birthDate" column="birth_date" jdbcType="OTHER"/>
        <result property="firstName" column="first_name" jdbcType="VARCHAR"/>
        <result property="lastName" column="last_name" jdbcType="VARCHAR"/>
        <result property="gender" column="gender" jdbcType="OTHER"/>
        <result property="hireDate" column="hire_date" jdbcType="OTHER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="EmployeesMap">
        select
          emp_no, birth_date, first_name, last_name, gender, hire_date
        from employees.employees
        where emp_no = #{empNo}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="EmployeesMap">
        select
          emp_no, birth_date, first_name, last_name, gender, hire_date
        from employees.employees
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="EmployeesMap">
        select
          emp_no, birth_date, first_name, last_name, gender, hire_date
        from employees.employees
        <where>
            <if test="empNo != null">
                and emp_no = #{empNo}
            </if>
            <if test="birthDate != null">
                and birth_date = #{birthDate}
            </if>
            <if test="firstName != null and firstName != ''">
                and first_name = #{firstName}
            </if>
            <if test="lastName != null and lastName != ''">
                and last_name = #{lastName}
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="hireDate != null">
                and hire_date = #{hireDate}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="empNo" useGeneratedKeys="true">
        insert into employees.employees(birth_date, first_name, last_name, gender, hire_date)
        values (#{birthDate}, #{firstName}, #{lastName}, #{gender}, #{hireDate})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update employees.employees
        <set>
            <if test="birthDate != null">
                birth_date = #{birthDate},
            </if>
            <if test="firstName != null and firstName != ''">
                first_name = #{firstName},
            </if>
            <if test="lastName != null and lastName != ''">
                last_name = #{lastName},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="hireDate != null">
                hire_date = #{hireDate},
            </if>
        </set>
        where emp_no = #{empNo}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from employees.employees where emp_no = #{empNo}
    </delete>

</mapper>